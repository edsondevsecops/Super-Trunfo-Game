# Requisitos

VS Code IDE ou qualquer outro editor de código

Instalação no IDE linguagem C/C++ e C/C++ Extension Pack (Microsoft)

Compilador windows: MinGW-w64 (baseado em GCC) ou MSVC (da Microsoft)

Compilador linux: GCC(GNU Compiler Collection)